/*
 *
 * Clapkit
 * ----------------------------------------------------------------------
 * A wrapper for creating a 'generalized' app for Classic MacOS
 * that (hopefully) can be ported easily to other platforms.
 *
 * CKGlobals
 * ----------------------------------------------------------------------
 * A (hopefully very short) list of global variables.
 */

class CKApp;
extern CKApp* __ckgCurrentCKApp;